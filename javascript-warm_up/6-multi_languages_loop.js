#!/usr/bin/node
// JS can iterate through strings with a for loop, uses 'let' for conditions
const text = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < 3; i++) {
  console.log(text[i]);
}
